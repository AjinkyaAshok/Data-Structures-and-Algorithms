let arr = [2, 3, 7, 3, 1, 8];

function insertionSort(arr) {
  let n = arr.length;
  for (let i = 1; i < n; i++) {
    let curr = arr[i];
    let prev = i - 1;

    while(prev>curr&& prev >=0){
        a[prev+1]
    }
  }
}
